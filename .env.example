# Copy to .env and fill in your key. The real .env is gitignored.
#
# Munney's import auditor is a MANDATORY AI agent: every CSV or Markdown file
# you import is verified by DeepSeek to confirm the transactions it will create
# faithfully match the file before anything is written to your budget.
#
# Get a key at https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# Optional overrides (defaults shown):
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_BASE_URL=https://api.deepseek.com
